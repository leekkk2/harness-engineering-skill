{
  "$schema": "drift-rules/v1",
  "description": "Architecture drift detection rule definitions. Customize layers and forbidden_patterns per project.",
  "layers": {
    "controller": {
      "can_import": ["service", "validator", "types", "config", "utils"],
      "cannot_import": ["controller"],
      "description": "Controller layer: handles HTTP requests/responses, delegates to services"
    },
    "service": {
      "can_import": ["types", "config", "utils"],
      "cannot_import": ["controller", "route"],
      "description": "Service layer: core business logic"
    },
    "validator": {
      "can_import": ["types"],
      "cannot_import": ["controller", "service", "config"],
      "description": "Validator layer: input validation and transformation"
    }
  },
  "forbidden_patterns": [
    {
      "pattern": "TODO|FIXME|HACK",
      "severity": "warning",
      "message": "Pending marker found — confirm whether action is needed"
    }
  ],
  "custom_rules": [
    {
      "id": "no-hardcoded-secrets",
      "pattern": "(password|secret|api_key|token)\\s*=\\s*['\"][^'\"]+['\"]",
      "exclude": ["*.test.*", "*.spec.*", "*.example.*"],
      "severity": "error",
      "message": "Hardcoded secrets are forbidden — use environment variables"
    }
  ]
}
